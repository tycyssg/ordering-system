server:
  port: 8085

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/online-orders?useUnicode=true&useLegacyDatetimeCode=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false #change this to your database url
    password: tycyisone90*T
    username: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
